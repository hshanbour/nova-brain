import test from "node:test";import assert from "node:assert/strict";import {classifyVoiceControlIntent,VOICE_CONTROL_INTENT} from "../src/voice/voice-control-intent.js";
const classify=(transcript,state)=>classifyVoiceControlIntent({transcript,state}).intent;
test("VoiceControl normalizes exact and bounded fuzzy pause commands only while speaking",()=>{for(const text of ["استني","إستنى!","وقّفي شوي","wait","hold on","استنّي"] )assert.equal(classify(text,"speaking"),VOICE_CONTROL_INTENT.PAUSE,text);assert.notEqual(classify("wait","listening"),VOICE_CONTROL_INTENT.PAUSE);});
test("VoiceControl normalizes exact and bounded fuzzy resume commands only while paused",()=>{for(const text of ["كملي","كمّل","كمل","كملي حكي","تابعي","يلا كملي","continue","carry on","keep going","كملّي"] )assert.equal(classify(text,"paused_waiting_for_user"),VOICE_CONTROL_INTENT.RESUME,text);assert.notEqual(classify("كملي","speaking"),VOICE_CONTROL_INTENT.RESUME);});
test("VoiceControl separates new conversation background and uncertainty",()=>{assert.equal(classify("كيف وضع المشروع اليوم","paused_waiting_for_user"),VOICE_CONTROL_INTENT.NEW_CONVERSATION);assert.equal(classify("people are talking about tomorrow outside","speaking"),VOICE_CONTROL_INTENT.UNRELATED);assert.equal(classify("music","paused_waiting_for_user"),VOICE_CONTROL_INTENT.UNCERTAIN);});
