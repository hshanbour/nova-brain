import {createHash} from "node:crypto";

const SHA=/^[a-f0-9]{40}$/;
const PROTECTED=/(^|\/)(src\/voice|speaker-worker|assets\/(?:voice-(?!input(?:\.|$))|speaker-)|\.github|api\/index\.js|src\/(?:policy|storage|autonomy))(\/|$)|ecapa|elevenlabs|voice-control|production|credential|secret|token/i;
const stable=value=>Array.isArray(value)?value.map(stable):value&&typeof value==="object"?Object.fromEntries(Object.keys(value).sort().map(key=>[key,stable(value[key])])):value;
const hash=value=>createHash("sha256").update(JSON.stringify(stable(value))).digest("hex");
const plannerError=(code,message,issues=[])=>Object.assign(new Error(message),{code,retryable:false,safeDiagnostics:{validationIssues:[...new Set(issues)].slice(0,20),outputShapeHash:null}});
const safePath=value=>{const path=String(value||"").trim().replaceAll("\\","/");if(!path||path.length>240||path.startsWith("/")||path.includes(".."))throw plannerError("implementation_plan_invalid","Implementation plan contains an invalid path.",["invalid_path"]);return path;};
const parse=value=>{const raw=String(value||"").trim(),text=/^```(?:json)?\s*[\s\S]*\s*```$/i.test(raw)?raw.replace(/^```(?:json)?\s*/i,"").replace(/\s*```$/,""):raw;try{return{value:JSON.parse(text),shapeHash:hash(text)}}catch{return{issues:["invalid_json"],shapeHash:hash(text)}}};
const TEST_PATH=/^test\/[a-z0-9._/-]+\.test\.js$/i;
const pathTokens=value=>new Set(String(value).toLowerCase().replace(/\.test(?=\.js$)/,"").replace(/\.[^.]+$/,"").split(/[^a-z0-9]+/).filter(part=>part.length>2&&!new Set(["test","tests","asset","assets","console"]).has(part)));
const discoveredPaths=steps=>{const found=new Set();const visit=value=>{if(typeof value==="string"&&/^[a-z0-9._/-]+$/i.test(value)&&value.includes("/"))found.add(value.replaceAll("\\","/"));else if(Array.isArray(value))value.forEach(visit);else if(value&&typeof value==="object")Object.values(value).forEach(visit);};for(const step of steps.filter(item=>item.status==="completed"&&["inspect_repo","search_code","read_files"].includes(item.stepType)))visit(step.result);return found;};

export const SELF_DEVELOPMENT_IMPLEMENTATION_PLAN_SCHEMA=Object.freeze({
  type:"object",additionalProperties:false,required:["summary","files","focusedTests","acceptanceMapping","riskLevel"],
  properties:{
    summary:{type:"string",minLength:1,maxLength:1000},
    files:{type:"array",minItems:1,maxItems:8,items:{type:"object",additionalProperties:false,required:["path","reason","operation","content","intendedChanges"],properties:{path:{type:"string",minLength:1,maxLength:240},reason:{type:"string",minLength:1,maxLength:500},operation:{type:"string",enum:["replace"]},content:{type:"string",maxLength:250000},intendedChanges:{type:"array",maxItems:20,items:{type:"string",minLength:1,maxLength:300}}}}},
    focusedTests:{type:"array",minItems:1,maxItems:12,items:{type:"string",minLength:1,maxLength:240}},
    acceptanceMapping:{type:"array",minItems:1,maxItems:30,items:{type:"object",additionalProperties:false,required:["criterion","files"],properties:{criterion:{type:"string",minLength:1,maxLength:500},files:{type:"array",minItems:1,maxItems:8,items:{type:"string",minLength:1,maxLength:240}}}}},
    riskLevel:{type:"string",enum:["low","medium"]}
  }
});

function schemaIssues(value){const issues=[];if(!value||typeof value!=="object"||Array.isArray(value))return["top_level_object_required"];const allowed=new Set(Object.keys(SELF_DEVELOPMENT_IMPLEMENTATION_PLAN_SCHEMA.properties));for(const key of Object.keys(value))if(!allowed.has(key))issues.push(`unknown_top_level:${key}`);if(typeof value.summary!=="string"||!value.summary.trim()||value.summary.length>1000)issues.push("summary_invalid");if(!Array.isArray(value.files)||value.files.length<1||value.files.length>8)issues.push("files_invalid");else value.files.forEach((file,index)=>{if(!file||typeof file!=="object"||Array.isArray(file)){issues.push(`file_${index}_object_required`);return;}const keys=new Set(["path","reason","operation","content","intendedChanges"]);for(const key of Object.keys(file))if(!keys.has(key))issues.push(`file_${index}_unknown:${key}`);if(typeof file.path!=="string")issues.push(`file_${index}_path_invalid`);if(typeof file.reason!=="string"||!file.reason.trim())issues.push(`file_${index}_reason_invalid`);if(file.operation!=="replace")issues.push(`file_${index}_operation_invalid`);if(typeof file.content!=="string"||file.content.length>250000)issues.push(`file_${index}_content_invalid`);if(!Array.isArray(file.intendedChanges)||file.intendedChanges.length>20||file.intendedChanges.some(item=>typeof item!=="string"||!item.trim()||item.length>300))issues.push(`file_${index}_intended_changes_invalid`);});if(!Array.isArray(value.focusedTests)||!value.focusedTests.length||value.focusedTests.some(item=>typeof item!=="string"))issues.push("focused_tests_invalid");if(!Array.isArray(value.acceptanceMapping)||!value.acceptanceMapping.length)issues.push("acceptance_mapping_invalid");else value.acceptanceMapping.forEach((item,index)=>{if(!item||typeof item.criterion!=="string"||!item.criterion.trim()||!Array.isArray(item.files)||!item.files.length||item.files.some(path=>typeof path!=="string"))issues.push(`acceptance_mapping_${index}_invalid`);});if(!["low","medium"].includes(value.riskLevel))issues.push("risk_level_invalid");return[...new Set(issues)].slice(0,20);}
const canonicalize=value=>{if(!value||typeof value!=="object"||Array.isArray(value))return value;const result={...value};if(result.acceptanceMapping===undefined&&Array.isArray(result.acceptanceCriteriaMapping))result.acceptanceMapping=result.acceptanceCriteriaMapping;if(result.riskLevel===undefined&&typeof result.risk==="string")result.riskLevel=result.risk;if(result.focusedTests===undefined&&Array.isArray(result.tests)&&result.tests.every(item=>typeof item==="string"))result.focusedTests=result.tests;delete result.acceptanceCriteriaMapping;delete result.risk;delete result.tests;return result;};

export function createSelfDevelopmentImplementationPlanner({modelProvider,storage,ownerId}={}){
  if(!modelProvider||!storage||!ownerId)throw new Error("Self-development implementation planner dependencies are required.");
  async function generate({taskId,candidatePaths,currentCommit}){
    if(typeof taskId!=="string"||!Array.isArray(candidatePaths)||candidatePaths.length<2||candidatePaths.length>21||!SHA.test(currentCommit||""))throw plannerError("implementation_plan_invalid","Exact task, commit, and bounded candidate paths are required.");
    const task=await storage.getAutonomyTask(taskId,ownerId);if(!task||task.taskType!=="self_development"||task.currentCommit!==currentCommit||task.branch!=="feat/nova-brain-mvp-foundation")throw plannerError("implementation_plan_precondition_failed","Implementation planning task binding changed.");
    const candidates=[...new Set(candidatePaths.map(safePath))];if(candidates.some(path=>PROTECTED.test(path)))throw plannerError("protected_scope_requires_approval","Protected Voice or runtime architecture cannot be planned by this recovery.");
    const steps=await storage.listAutonomySteps(task.id),reads=new Map();for(const step of steps.filter(item=>item.stepType==="read_files"&&item.status==="completed")){const path=step.result?.path||step.input?.arguments?.path;if(path&&typeof step.result?.content==="string"&&!step.result?.truncated)reads.set(path,step.result.content);}if(candidates.some(path=>!reads.has(path)))throw plannerError("implementation_evidence_incomplete","Every candidate file must be read completely before implementation planning.");
    const request=task.metadata?.selfDevelopment,evidence=candidates.map(path=>({path,content:reads.get(path)}));if(JSON.stringify(evidence).length>180000)throw plannerError("implementation_evidence_too_large","Implementation evidence exceeds the bounded planner context.");const availableEvidenceExpansionTests=[...discoveredPaths(steps)].filter(path=>TEST_PATH.test(path)&&!PROTECTED.test(path)&&!reads.has(path)).slice(0,24);
    const prompt={taskId:task.id,currentCommit,userGoal:request.userGoal,acceptanceCriteria:request.acceptanceCriteria,candidateFiles:evidence,availableEvidenceExpansionTests},seen=new Set();let feedback=[];
    for(let attempt=0;attempt<3;attempt++){
      const generated=await modelProvider.generate({message:`Create the bounded implementation plan.\n${JSON.stringify(prompt)}${feedback.length?`\nCorrect only these schema issues from the prior output: ${feedback.join(", ")}.`:""}`,conversationHistory:[],context:{},tools:[],responseFormat:{name:"nova_self_development_implementation_plan",schema:SELF_DEVELOPMENT_IMPLEMENTATION_PLAN_SCHEMA,strict:true},systemContext:"You are Nova's Self-Development implementation planner. Return exactly the requested schema. files use complete replacement content and operation=replace and may use only candidateFiles. focusedTests may use evidence-read candidateFiles or directly relevant paths listed in availableEvidenceExpansionTests; unread tests will be read before execution and the plan will be regenerated. acceptanceMapping files must reference generated patch files. Never target Voice Mode, ECAPA, ElevenLabs, speaker verification, Voice controls, main, or Production. Never use shell commands or secrets."});
      let shape=null;if(generated?.type!=="final")feedback=["final_structured_result_required"];else{const parsed=parse(generated.message);shape=parsed.shapeHash;if(parsed.issues)feedback=parsed.issues;else{const value=canonicalize(parsed.value),issues=schemaIssues(value);if(!issues.length)return build(value,{task,steps,candidates,reads,request,evidence});feedback=issues;}}
      if(shape&&seen.has(shape))throw invalid(feedback,shape,attempt+1);if(shape)seen.add(shape);if(attempt===2)throw invalid(feedback,shape,attempt+1);
    }
  }
  function invalid(issues,shapeHash,attempts){const error=plannerError("implementation_plan_invalid","Nova implementation plan does not match the bounded schema.",issues);error.safeDiagnostics={validationIssues:issues.slice(0,20),outputShapeHash:shapeHash,formatAttempts:attempts};return error;}
  function build(value,{task,steps,candidates,reads,request,evidence}){const files=value.files.map(file=>{const path=safePath(file.path);if(!candidates.includes(path)||PROTECTED.test(path))throw plannerError("implementation_scope_violation","Nova proposed a file outside the evidence-bound scope.",["evidence_bound_path_required"]);return{path,operation:"replace",content:file.content,expectedContent:reads.get(path),reason:file.reason.slice(0,500),intendedChanges:file.intendedChanges.slice(0,20).map(item=>item.slice(0,300))};});const paths=new Set(files.map(file=>file.path)),focusedTests=[...new Set(value.focusedTests.map(safePath))],unread=focusedTests.filter(path=>!reads.has(path));if(unread.length){const history=task.metadata?.implementationEvidenceExpansionHistory||[],discovered=discoveredPaths(steps),relevantTokens=new Set([...candidates.filter(path=>!path.startsWith("test/")).flatMap(path=>[...pathTokens(path)]),...pathTokens(request.userGoal)]);if(history.length>=3)throw plannerError("implementation_evidence_expansion_limit","Focused-test evidence expansion limit reached.",["focused_test_evidence_expansion_limit"]);if(unread.length>3)throw plannerError("implementation_scope_violation","Focused-test evidence expansion is too large.",["focused_test_evidence_too_large"]);for(const path of unread){const overlap=[...pathTokens(path)].some(token=>relevantTokens.has(token));if(!TEST_PATH.test(path)||PROTECTED.test(path)||!discovered.has(path)||!overlap)throw plannerError("implementation_scope_violation","Focused test is not eligible for bounded evidence expansion.",["focused_test_evidence_rejected"]);}const pathHashes=unread.map(path=>hash(path)),prior=new Set(history.flatMap(item=>item.pathHashes||[]));if(pathHashes.every(value=>prior.has(value)))throw plannerError("implementation_evidence_expansion_repeated","Repeated focused-test evidence expansion was rejected.",["focused_test_evidence_repeated"]);return{ok:true,evidenceExpansion:{code:"focused_test_evidence_required",category:"focused_test",attempt:history.length+1,paths:unread,pathHashes,candidatePaths:[...new Set([...candidates,...unread])]}};}if(focusedTests.some(path=>!candidates.includes(path)||!TEST_PATH.test(path)||PROTECTED.test(path)))throw plannerError("implementation_scope_violation","Focused tests must be evidence-read candidate test files.",["focused_test_evidence_required"]);for(const mapping of value.acceptanceMapping)if(mapping.files.some(path=>!paths.has(path)))throw plannerError("implementation_plan_invalid","Acceptance mapping must reference only generated patch files.",["acceptance_mapping_path_invalid"]);const acceptanceMapping=value.acceptanceMapping.map(item=>({criterion:item.criterion.slice(0,500),files:item.files}));return{ok:true,implementationPlan:{files,focusedTests,acceptanceMapping,riskLevel:value.riskLevel,summary:value.summary.slice(0,1000),evidencePaths:candidates,goalHash:hash(request.userGoal),evidenceHash:hash(evidence),planHash:hash({files:files.map(({expectedContent,...file})=>file),focusedTests,acceptanceMapping,riskLevel:value.riskLevel})}};}
  return Object.freeze({generate});
}

export const SELF_DEVELOPMENT_PLANNER_PROTECTED=PROTECTED;
